
// PRODUCTION BUILD: FIREBASE REMOVED
// The system now uses the Deterministic Simulation Engine (services/simulationEngine.ts)
// This ensures 100% uptime and consistent data for demonstrations without external dependencies.

const db: any = null;
const isFirebaseConfigured = false;

export { db, isFirebaseConfigured };
